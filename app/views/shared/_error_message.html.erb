<% if @user.errors.any? %>
	<div id="errors">
		The registration form contains <%= pluralize(@user.errors.count, "error") %>.
		<ul id="error list">
			<% @user.errors.full_message.each do |msg|%>
				<li><%= msg %></li>
				<% end %>
			</ul>
		</div>
		<% end%>
		